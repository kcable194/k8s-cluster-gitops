---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: arc
spec:
  chartRef:
    kind: OCIRepository
    name: arc
  interval: 1h
  values:
    replicaCount: 1
    resources:
      requests:
        cpu: 100m
      limits:
        memory: 256Mi
